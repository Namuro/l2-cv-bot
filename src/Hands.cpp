#include "Hands.h"

void Hands::MoveMouse(int x, int y) const
{
    //const int deltaX = abs(x2 - x1);
    //const int deltaY = abs(y2 - y1);
    //const int signX = x1 < x2 ? 1 : -1;
    //const int signY = y1 < y2 ? 1 : -1;
    ////
    //int error = deltaX - deltaY;
    ////
    //setPixel(x2, y2);
    //while (x1 != x2 || y1 != y2)
    //{
    //    setPixel(x1, y1);
    //    const int error2 = error * 2;
    //    //
    //    if (error2 > -deltaY)
    //    {
    //        error -= deltaY;
    //        x1 += signX;
    //    }

    //    error += deltaX;
    //    y1 += signY;
    //}
}
